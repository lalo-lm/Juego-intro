import greenfoot.*;
 
/**
 * SafeZone: área verde que actúa como checkpoint y zona de inicio/fin.
 * El Player detecta la intersección y actualiza su punto de respawn.
 * Si el jugador llega aquí con todas las monedas recogidas → nivel completado.
 */
public class SafeZone extends Actor {
 
    private static final int WIDTH  = 120;
    private static final int HEIGHT = 120;
 
    public SafeZone() {
    }
 
 
    @Override
    public void act() {
        // Lógica manejada desde Player
    }
}
 